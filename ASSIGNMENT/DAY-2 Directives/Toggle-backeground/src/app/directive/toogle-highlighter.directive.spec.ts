import { ToogleHighlighterDirective } from './toogle-highlighter.directive';

describe('ToogleHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
